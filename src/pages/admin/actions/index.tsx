import React from 'react';
import ActivityHistory from 'components/admin/common/ActivityHistory';

const Actions = () => {
    return (
        <div className="site-layout-background">
            <ActivityHistory PropsType="Actions" />
        </div>
    );
};

export default Actions;
